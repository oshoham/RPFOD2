Square(Grid gr, Vector2 loc, Vector3 wloc) 							- Oren

	Vector2 loc = location coordinates on grid 						-Oren
	Vector3 wloc = world coordinates -Oren
	Square[8] bord = squares bordering given square						 -Oren
		0 = SW, move clockwise
	List<GameObject> obj = gameobjects in square						 -Oren

	Other methods
		bool Move(Square sq, GameObject stuf)						 - Kyle
			moves stuf to square, return true if org. square had object
	//Needs
	update()?
		


Grid(int width, int height)									-Oren
	
	Square[,] grid = 2D array of squares with x, y coordinates				-Oren
	int width, height = width/height of grid						-Oren
	
	Methods
		bool Check(Vector2 loc)								-Oren
			return true if objects are in square
		bool CheckWall(Vector2 lov)							-Oren
			return true if square occupied by Wall
		GameObject Get(Vector2 loc, string type)					-Oren
			returns first inst. of given obj. in Square at loc
		bool Move(Vector2 s, Vector2 e, GameObject move)				-Oren/Kyle
			returns if object was successfully moved
			
